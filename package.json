{
  "name": "chart-merger",
  "version": "0.1.1",
  "description": "Merge data from different streams of data with `x` and `y` keys.",
  "main": "index.js",
  "repository": "https://github.com/theodorton/chart-merger",
  "scripts": {
    "test": "mocha --compilers js:babel-register --recursive test",
    "prepublish": "babel -d build src"
  },
  "engines": {
    "node": ">=4 <5"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.3.17",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "mocha": "^2.3.4"
  },
  "files": [
    "index.js",
    "build"
  ]
}
